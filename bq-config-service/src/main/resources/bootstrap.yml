spring:
  application:
    name: bq-config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pearpai/spring-cloud/bq-config-service/src/main/resources/service-config
          search-paths: service-config
#          username: ******   如果是私有仓库需要 填写用户 密码
#          password: ******

server:
  port: 8006

eureka:
  client:
    service-url:
      defaultZone: http://user:password123@localhost:8761/eureka


