---
spring:
  application:
    name: bq-client-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: bq-config-service

eureka:
  client:
    service-url:
      defaultZone: http://user:password123@localhost:8761/eureka


